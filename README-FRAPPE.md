This branch is copy of dev branch of the repository. This has been created to introduce the mcp for Frappe in Python SDK.
Dockerfile is modified to clone the github.com/gsnbng/frappe-python-mcp.git through docker-entrypoint.sh.
LibreChat.yml file is modified to include the mcp server FrappeStockItemService config in stdio mode.
